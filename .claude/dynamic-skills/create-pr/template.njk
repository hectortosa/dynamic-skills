Create a pull request for the current branch.

**Title format:** Follow conventional commits (e.g. `feat: add user authentication`, `fix: resolve null pointer in parser`).

**Body:** Use the following PR template exactly:

```markdown
{{ pr_template }}
```

Fill in each section based on the actual changes in the branch. Use `gh pr create` to create the PR.
